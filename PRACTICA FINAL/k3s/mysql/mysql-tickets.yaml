apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-tickets
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-tickets
  template:
    metadata:
      labels:
        app: mysql-tickets
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "201504070"
            - name: MYSQL_DATABASE
              value: "tickets_db"
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-tickets
spec:
  selector:
    app: mysql-tickets
  ports:
    - port: 3306