apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-users
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-users
  template:
    metadata:
      labels:
        app: mysql-users
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "201504070"
            - name: MYSQL_DATABASE
              value: "users_db"
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-users
spec:
  selector:
    app: mysql-users
  ports:
    - port: 3306